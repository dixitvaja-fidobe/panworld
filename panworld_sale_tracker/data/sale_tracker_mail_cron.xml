<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_sale_tracker_mail_crone" model="ir.cron">
        <field name="name">Sale Tracker: Sent mail to responsible user when sale TAT Breached from SO</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">model._trigger_sale_tat_breached_so()</field>
    </record>
</odoo>
