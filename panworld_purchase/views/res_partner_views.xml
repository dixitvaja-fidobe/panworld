<odoo>
    <record id="res_ppartner_form_extended_panworld" model="ir.ui.view">
        <field name="name">res.partner.extended.panworld</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="purchase.view_partner_property_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='property_purchase_currency_id']"
                position="after">
                <field name="is_validation_exempt" />
            </xpath>
            <xpath expr="//group[@name='purchase']" position="inside">
                <field name="default_supplierinfo_discount"
                    invisible="is_company == False and parent_id != False"/>
                <div name="supplierinfo_discount_disabled"
                    colspan="2"
                    invisible="is_company == True or parent_id == False">
                    <p>Discount-related settings are managed on <button
                            name="open_commercial_entity"
                            type="object"
                            string="the parent company"
                            class="oe_link"/></p>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
