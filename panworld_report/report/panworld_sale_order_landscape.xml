<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Paper Format Landscape -->
    <record id="paperformat_saleorder_landscape" model="report.paperformat">
        <field name="name">Sale Order Landscape</field>
        <field name="default" eval="True"/>
        <field name="format">A4</field>
        <field name="page_height">0</field>
        <field name="page_width">0</field>
        <field name="orientation">Landscape</field>
        <field name="margin_top">10</field>
        <field name="margin_bottom">10</field>
        <field name="margin_left">7</field>
        <field name="margin_right">7</field>
        <field name="header_line" eval="False"/>
        <field name="header_spacing">35</field>
        <field name="dpi">90</field>
    </record>

    <!-- Report Action Landscape -->
    <record id="action_report_saleorder_landscape" model="ir.actions.report">
        <field name="name">Quotation / Order (Landscape)</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">panworld_report.report_saleorder_landscape</field>
        <field name="report_file">panworld_report.report_saleorder_landscape</field>
        <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id" ref="panworld_report.paperformat_saleorder_landscape"/>
    </record>

    <!-- Report Template Landscape -->
    <template id="report_saleorder_landscape">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="sale.report_saleorder_document" t-lang="doc.partner_id.lang"/>
            </t>
        </t>
    </template>

</odoo>
