<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="wizard_stock_ageing_report_form_view" model="ir.ui.view">
            <field name="name">wizard.stock.ageing.report.view</field>
            <field name="model">wizard.stock.ageing.report</field>
            <field name="arch" type="xml">
                <form>
                    <field name="state" invisible="1"/>
                    <field name="name" invisible="1"/>
                    <group invisibel="state != 'choose'">
                        <field name="company_id" required="1" groups="base.group_multi_company"
                               options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" />
                    </group>
                    <group invisible="state != 'choose'">
                        <group>
                            <field name="start_date" required="1"/>
                        </group>
                    </group>
                    <group invisibel="state != 'choose'">
                        <field name="warehouse_ids" widget="many2many_tags"
                            domain="[('company_id','=',company_id)]" required="1"
                            options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" />
                        <field name="location_ids" widget="many2many_tags"
                            domain="[('company_id', '=', company_id), ('usage', '=', 'internal')]"
                            options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" />
                    </group>

                    <group invisibel="state != 'choose'">
                        <group>
                            <field name="group_by_categ"/>
                        </group>
                        <group>
                            <field name="filter_by"/>
                        </group>
                    </group>
                    <notebook invisible="filter_by == False or filter_by == 'category'">
                        <page string="Product" invisibel="state != 'choose'">
                            <group>
                                <field name="product_ids"
                                        widget="many2many_tags" domain="['|', ('type', '=', 'product'), ('is_storable', '=', True)]"
                                       options="{'no_create':True,'no_edit':True}"/>
                            </group>
                        </page>
                    </notebook>
                    <notebook invisible="filter_by == False or filter_by == 'product'">
                        <page string="Category" invisibel="state != 'choose'">
                            <group>
                                <field name="category_ids" widget="many2many_tags" 
                                       options="{'no_create':True,'no_edit':True}"/>
                            </group>
                        </page>
                    </notebook>

                    <div invisible="state != 'get'">
                            <h2>Report Download</h2>
                            <p>File : <field name="data" readonly="1" filename="name"/></p>
                    </div>
                    <footer invisible="state != 'choose'">
                        <button name="print_report" string="PDF" type="object" class="oe_highlight" />
                        or
                        <button name="print_xls_report" string="XLS" type="object" class="oe_highlight" />
                        or
                        <button string="Cancel" class="oe_link" special="cancel" />
                    </footer>
                    <footer invisible="state != 'get'">
                        <button name="go_back" string="Go Back" type="object" class="oe_highlight" />
                        or
                        <button special="cancel" string="Close" type="object" class="btn-primary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_wizard_stock_ageing_report">
            <field name="name">Stock Ageing Report</field>
            <field name="res_model">wizard.stock.ageing.report</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="wizard_stock_ageing_report_form_view" />
            <field name="target">new</field>
        </record>

        <menuitem action="action_wizard_stock_ageing_report" id="menu_wizard_stock_ageing_report"
                  parent="stock.menu_warehouse_report" sequence="2"/>

    </data>
</odoo>
